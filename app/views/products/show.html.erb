<h2>
<%= @product.title %>
</h2>
<%= image_tag @product.product_image_url(), :style => "max-height: 100%; max-width: 100%;" %>

<p class="date">Submitted <%= time_ago_in_words(@product.created_at) %> ago</p>

<p class="body"><%= @product.body %> </p>
